# ============================================================
# tank-world ENV — 项目级静态配置
# 基于 agent-dev-standard templates/env.yaml.template（github 流派，TAPD 段已删）
# 维护方: PM / 详细 spec: <standard_path>/docs/concepts/project-env-spec.md
# ============================================================

project:
  name: tank-world
  flow_type: github
  code_repo: tank-world

github:
  issue_repo: "Corray/tank-world"
  active_milestones: []

env:
  # 浏览器端游戏，暂无 dev/staging/prod 服务环境；引入后按段补
  dev: "[TBD]"
