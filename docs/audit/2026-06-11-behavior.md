# Audit 报告 — behavior phase（2026-06-11 第二轮）

**范围：** 共识 v12 AC-1~95 ↔ tests/（27 个 spec 文件 / 277 块）+ test-plan/（13 份 + INDEX）+ CI（ci.yml）+ S2 安全基线 + R11~R13 骨架锁定抽查
**上轮基线：** 2026-06-05 首轮（AC-1~46 / 175 块，3 发现全 resolved）
**结论：** 4 发现（2 MEDIUM / 2 LOW，全为流程类）；**AC 覆盖无 ❌ 缺口**（86 ✅ / 8 🔶 豁免或浏览器冒烟 / 1 已取代）

---

## 一、发现清单

| 编号 | 严重度 | 分类 | 发现 |
|------|--------|------|------|
| F-PROC-20260611-999d | MEDIUM | 风险（时限 5 天） | **CI Actions Node20 运行时弃用未处理**：`ci.yml` 仍用 `actions/checkout@v4` / `setup-node@v4`（×2 job）/ `upload-pages-artifact@v3`（Node20 运行时家族），GitHub 自 **2026-06-16** 起强制以 Node24 运行——届时 CI 行为可能出现警告升级/兼容性破坏，而 CI 是本项目唯一 required check（test）+ 部署管道，红灯即阻断全部合入。次级证据：`node-version: 20` 工具链已过 Node 20 维护期（2026-04-30 EOL）。位置：`.github/workflows/ci.yml:14,15,34,35,41,17,37`。建议：升级 `checkout@v5` / `setup-node@v5` / `upload-pages-artifact@v4`（或当期最新），`node-version` 提至 22/24，并在 6-16 前合入验证一轮绿灯 |
| F-PROC-20260611-514f | MEDIUM | 偏差（停更复发） | **项目门面文档停更复发**（F-PROC-20260605-6fd2 同家族，resolved 后再次漂移）：① `CLAUDE.md`「当前 active 阶段」仍写「六轮迭代完成…PR #1~#10 全合入」「R7 候选池」——实际已 13 轮 / PR #23；② `README.md`（AC-46 表面）仍写「五轮迭代 / 共识 v5 / 5 份测试计划 / 四档最高分 / 3 种道具 / 8 成就」，键位表缺 `3/4/5/6`（VS/MELEE/WAVE 入口），且声称「实时数字见 CI 徽标」但 README 内**无任何 CI 徽标**——实际现状：13 轮 / 共识 v12 / 13 份计划 / 八档 / 7 道具 / 5 模式。同根因 = 轮次收尾流程无「门面文档同步」步骤，**修一次漂一次**，应机制化（见系统性建议 §三.1） |
| F-PROC-20260611-a872 | LOW | 偏差（状态滞后） | `docs/spec/test-plan/INDEX.md:16` R13 行状态仍标「待 PR 合入」，PR #23 已于 master 合入（merge 98316c2）——状态写于 PR 内（9dd37f7），合入后无人回写。计数链本身一致（声明 277 = 实测 277） |
| F-PROC-20260611-6ac3 | LOW | 改进建议（审计载体） | R13 骨架修正（wave.spec.ts T-WAV-6）**混入实现 commit**（3a94fba feat）而非独立 commit：修正本身合规（见 §二.4——仅改驱动方式、断言零改、inline 留痕注释带日期与理由），但 test-skeleton-lock 的机器化校验锚是 `git diff <骨架commit> -- <骨架文件>` 为空，一旦非空，混在 feat commit 中的修正需逐 hunk 人工甄别才能区分「合法驱动修正 vs 篡改断言」。建议：后续骨架修正独立 commit（`test(skeleton-fix):` 前缀 + 修正理由），保持 feat commit 的骨架 diff 为空 |

---

## 二、正向/反向比对结论（无发现部分的证据）

### 2.1 正向：AC-1~95 覆盖（详表见执行日志）

- **86 条 ✅ 有单测覆盖**：13 份测试计划 §映射逐条核对至 tests/ 实际 describe 名，全部对得上（T-PLY/CMB/MAP/ENM/SM/LVL/AI/PWR/STO/FX/AU/EN/TER/MAPV/ACH/2P/CE/ACH2/VS/MEL/UP/BOSS/ITM/WAV 族）；其中 AC-51/59/67/75 为元验收（G3 分叉清单 grep 核验，计划 §4 有执行留痕）、AC-45 为 CI 机制验证（ci.yml + PR #18~#23 实跑实证）
- **8 条 🔶 浏览器冒烟/豁免**（定位裁定 v5 §1，P-20260605-88ee dismissed）：AC-8（HUD 目视）/ AC-9、AC-31（60fps）/ AC-10、AC-27（file:// 部署）/ AC-21（平衡实测，consensus 已标豁免注）/ AC-37（READY 成就进度显示）/ AC-46（README 目视——内容停更已另记 514f）
- **1 条已取代**：AC-44 被 v6 AC-47/49 改判取代（consensus 有取代标注；regression-coop issue #7 测试断言已反转）✓
- **❌ 无覆盖且非豁免：0 条**

### 2.2 反向：无锚测试

27 个 spec 文件全部 describe 块过查：T-* 族均可追溯到对应测试计划；regression-* 4 文件均有 issue 锚（#6/#7/#8/#13/#14）。**合理差集 2 处**（基础设施 sanity，非 AC 载体，维持上轮判定）：`level.spec.ts:198`（addEnemy helper sanity）、`game.spec.ts:166`（GameLoop fixed timestep accounting，对应数据模型循环契约）。

### 2.3 测试计数链

INDEX 声明链 60→94→120→147→166→186→210→225→241→249→263→277，逐轮增量与各轮声明自洽（R9 +15 / R10 +16 / R11 +8 / R12 +14 / R13 +14 均吻合）；**本轮实测 `npx vitest run` = 277/277 全绿（27 文件）**，与终值一致。README 不含硬编码测试数字（无计数漂移面）。

### 2.4 骨架锁定抽查（R11~R13）

| 轮 | 骨架 commit | 骨架文件 | `git diff <commit>..HEAD` | 判定 |
|----|------------|---------|--------------------------|------|
| R11 | 26a313e | tests/boss.spec.ts | **空** | ✅ 锁定完好 |
| R12 | 293e124 | tests/items.spec.ts | **空** | ✅ 锁定完好 |
| R13 | b003540 | tests/wave.spec.ts | 1 hunk（T-WAV-6） | ✅ 修正合规：单次大跨度 `loop.advance(WAVE_BREAK_MS−STEP)` 改为逐步推进循环（advance 层有 250ms tab-switch clamp，大跨度被钳与 rAF 真实驱动不符）；4 条 expect 断言（WAVE_BREAK 保持 / clock 冻结 / PLAYING / wave=2）逐字未动；inline 注释留痕含日期与理由。载体问题另记 6ac3（LOW） |

### 2.5 安全基线 S2（全项目唯一执行点）

`grep -rniE "key|secret|token|password|credential" src/`：命中全部为键盘事件（KeyW/keydown）、localStorage 存档键名（KEY_BEST_* 常量族）、Map 键等良性用法；`secret|password|credential|api-key|bearer` 在 src/ tests/ .github/ **零命中**。✅ 无硬编码凭证。S1/S3 维持 N/A 裁剪声明。

---

## 三、系统性建议

1. **轮次收尾 checklist 增「门面文档同步」步骤**（针对 514f 复发）：每轮 PR 合入后的实现总结 commit 中固定核对 CLAUDE.md「当前 active 阶段」+ README 数字面（轮数/共识版本/道具/模式/键位/档位）+ INDEX 状态行（针对 a872）。该模式已二次发生（6fd2 → 514f），符合 project-patterns 建档阈值——本项目尚无 `docs/problems/project-patterns.md`，建议借此建档首条 PP（tendency：实现侧文档随轮更新、门面/上下文文档无人触发）。
2. **CI 升级窗口管理**：999d 有硬时限（2026-06-16），建议本周内独立 chore PR 处理，勿与功能轮捆绑。
3. **骨架修正纪律固化**（针对 6ac3）：在项目级约定「骨架文件修正必须独立 commit + `test(skeleton-fix)` 前缀」，使 `git diff <骨架commit> -- <文件>` 非空时可直接按 commit 边界审计，无需逐 hunk 甄别。

---

> 本报告 immutable；状态流转见 findings-registry。proposals 见 `2026-06-11-behavior-proposals.md`。
